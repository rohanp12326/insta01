export const likePost = (postId) => {
    // Logic to like the post based on postId
    console.log(`Post ${postId} liked`);
  };
  
  export const openComments = (postId) => {
    // Logic to open the comment section of the post based on postId
    console.log(`Comments for post ${postId} opened`);
  };
  
  export const sharePost = (postId) => {
    // Logic to share the post based on postId
    console.log(`Post ${postId} shared`);
  };
  
  export const navigateToUserProfile = (username) => {
    // Logic to navigate to the user's profile based on username
    console.log(`Navigated to profile of ${username}`);
  };
  
  export const showOptionsMenu = (postId) => {
    // Logic to show the options menu for the post based on postId
    console.log(`Options menu for post ${postId} shown`);
  };
  
  
  